namespace DataStructures
{
    /**
    * The Weighted interface declares the methods required to implemented
    * weighted edges in a graph.
    * @author Zach Samuels
    */
    public interface IWeighted
    {
        /**
        * Gets the edge's weight.
        * @return The edge's weight.
        */
        int GetWeight();
    }
}
